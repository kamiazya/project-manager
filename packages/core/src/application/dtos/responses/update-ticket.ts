import { TicketResponse } from './ticket.ts'

/**
 * Response DTO for the unified ticket update operation.
 * Extends the standard TicketResponse to maintain consistency.
 */
export class UpdateTicketResponse extends TicketResponse {}
